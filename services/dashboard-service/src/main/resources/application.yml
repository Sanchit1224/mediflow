spring: { application: { name: dashboard-service }, config: { import: "optional:configserver:${CONFIG_SERVER_URI:http://localhost:8888}" } }
server: { port: 8086, shutdown: graceful }
eureka: { client: { service-url: { defaultZone: "${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}" } } }
management: { endpoints: { web: { exposure: { include: health,info,prometheus } } }, endpoint: { health: { probes: { enabled: true } } } }
